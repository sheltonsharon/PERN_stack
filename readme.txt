---PERN full stack application---

-->To run the application:
PORT
PGUSER
PGHOST
PGPASSWORD
PGDATABASE
PGPORT
These variables must be configured with the respective info from your postgres database
Port number is of your choice

Cd into run client and server separately with `npm run start`

----------------------------------
Notes:
dotenv npm makes it easier to manage environment variables

Extension installed:
ES7 React/Redux/GraphQL/React-Native snippets
-->imr in editor and import react line
-->imrd - reactDOM
--rafce - boilerplate for the component